
We need to add an overview, navigation, and semantic markup to some html files. The model file is "focus-advice-vs-advise.html". The files that we are changing are very similar to our model file, but just need to be tweaked and updated to the new style. 

For all instructions, efforts, etc., use the most efficient token means possible. 

1a. add the language attribute to the opening html tag:
<html lang="en">

1b. Replace the internal stylesheet with this external stylesheet: 
  <link rel="stylesheet" href="spectrum.css">

1c. <div class="hero"> should be header class <header class="hero"> 

2. Add the navigatino right after <div class="lesson-container"> division:
<nav class="main-nav">
  <div class="nav-container">
    <a href="#" class="nav-logo">✦︎ Top </a>

    <div class="nav-links">
      <a href="#overview" class="nav-item">Overview</a>
      <a href="#definitions" class="nav-item">Definitions</a>
      <a href="#usageTop" class="nav-item">Usage</a>
    </div>
  </div>
  
3. add an overview section with a generated simple overview based on the pages contents:
  <section id="overview">
  <h2 style="font-size:150%">&#128240;Overview </h2>
  <p> <!-- Claude Generated Article Overview--> </p>
  </section>
  
4. add a section element here:
   <section id="definitions">
    <div class="contrast-grid">
  to include all <div class="card">
  
5. add a sectin element here as well:
   <section id="usage">
    <div class="guidelines-grid">
    to include all <div class="guideline-item">
    
6. Add the ID #usageTop to the guidelines-wrapper:
<div id="usageTop" class="guidelines-wrapper">

7. remove all emoticons from the textContent of <h1> and trim it. in JS I might do something like this:
// Removes emojis and collapses multiple spaces into one then trim
document.querySelectorAll('h1').forEach(h1 => {
  h1.innerText = h1.innerText
    .replace(/\p{Emoji}/gu, '') // Removes emojis
    .replace(/\s+/g, ' ')       // Collapses extra spaces
    .trim();                    // Removes leading/trailing space
});
But you do it as you see fit. I just want to make sure you understand what I mean. 

